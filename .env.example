# Base URL of the real mengu-backend API.
# In local dev, Vite proxies /api/* to localhost:8080 (see vite.config.ts),
# so VITE_API_URL can stay relative and pointing at the proxy.
VITE_API_URL=/api/v1
